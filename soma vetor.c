#include <stdio.h>
int main()
{
    int i, vetor[15], maior,menor;
    printf("digite um numero:");
        scanf("%d", &vetor[i]);
        maior vetor[0];
        menor vetor[0];
    for(i=1; i<15; i++)
    {
        printf("digite um numero:");
        scanf("%d", &vetor[i]);
        if(vetor[i]>maior)
            maior=vetor[i];
        if(vetor[i]<menor)
            menor=vetor[i];
    }
    